# LawTune on Apple Silicon (M1/M2/M3/M4). Python 3.10-3.12.
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# No bitsandbytes, no unsloth: MLX does LoRA on 4-bit quantized weights natively.
# torch is here only for IndicTrans2 (step 02), which runs on MPS.

mlx>=0.18.0
mlx-lm>=0.19.0

torch>=2.4.0                 # IndicTrans2 only; MPS backend
transformers>=4.44.0
datasets>=2.18.0
huggingface_hub>=0.23.0
safetensors
sentencepiece
protobuf
pyarrow

# translation (step 02) -- IndicTrans2
IndicTransToolkit @ git+https://github.com/VarunGumma/IndicTransToolkit.git
sacremoses

# data ingest
kaggle
